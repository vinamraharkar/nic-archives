# Secrets
.env

# macOS
.DS_Store

# Large binary assets (too big for GitHub)
Archives/
docs/pages/
NIC Archives.pdf
document.md

# Eval — reports are per-run and not committed; baseline + judgments ARE committed
eval/reports/
eval/review.html

# Intermediate pipeline outputs (regenerable)
raw_text/
articles_visual/
eval_output/
classified.json
pipeline/errors.json
pipeline/embed_progress.json
pipeline/eval_results.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Claude Code internal
.claude/

# Netlify
.netlify

# Cloudflare Workers — contains account_id and database_id
workers/wrangler.toml
